package com.agilejava.blammo;

/**
 * A test logger interface. (Nothing but a marker interface for now. Note that
 * Blammo is not actually dynamically creating implementations of interfaces, so
 * we don't need to test if the signature of the operations matches the
 * signature of the implementation class.)
 * 
 * @author Wilfred Springer
 */
public class TestLogger {

}
